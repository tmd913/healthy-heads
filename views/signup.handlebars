    <div class="container" id="formContainer">
        <div class="row">
            <div class="col-md-6 col-md-offset-3">
                <h1>Sign Up Form</h1>

                <form class="profSignup">
                    <div class="input-field">
                        <label for="signup-email">Login Email</label>
                        <input type="email" class="validate email-input" name="signup-email">
                    </div>
                    <div class="input-field">
                        <label for="signup-password">Login Password</label>
                        <input type="password" class="validate password-input" name="signup-password">
                    </div>

                    <div style="display: none" id="alert" class="alert alert-danger" role="alert">
                        <span class="glyphicon glyphicon-exclamation-sign" aria-hidden="true"></span>
                        <span class="sr-only">Error:</span>
                        <span class="msg"></span>
                    </div>
                    <button type="submit" class="waves-effect waves-light btn blue lighten-2">Sign Up</button>
                </form>
                <br/>
                <p>Or log in
                    <a href="/"> here</a>
                </p>
            </div>
        </div>
    
    </div>
    {{!-- end container --}}